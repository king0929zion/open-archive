diff --git a/lib/main.dart b/lib/main.dart
--- a/lib/main.dart
+++ b/lib/main.dart
@@ -93,6 +93,7 @@ class ArchiveApp extends StatelessWidget {
       splashFactory: NoSplash.splashFactory,
       splashColor: Colors.transparent,
       highlightColor: Colors.transparent,
+      shadowColor: Colors.transparent,
       colorScheme: ColorScheme.fromSeed(
         seedColor: _button,
         surface: _bg,
@@ -452,8 +453,8 @@ class _ArchiveShellState extends State<ArchiveShell> {
                     children: [
                       AnimatedSwitcher(
-                        duration: _motionDuration(context, 230),
-                        reverseDuration: _motionDuration(context, 200),
-                        switchInCurve: Curves.easeOutCubic,
+                        duration: _motionDuration(context, 270),
+                        reverseDuration: _motionDuration(context, 235),
+                        switchInCurve: Curves.easeOutQuart,
                         switchOutCurve: Curves.easeInCubic,
@@ -468,19 +469,25 @@ class _ArchiveShellState extends State<ArchiveShell> {
                           final reverse = key is ValueKey<_PageTransitionToken>
                               ? key.value.reverse
                               : false;
                           final offset = reverse
-                              ? const Offset(-.045, 0)
-                              : const Offset(.045, 0);
+                              ? const Offset(-.026, 0)
+                              : const Offset(.026, 0);
                           final curved = CurvedAnimation(
                             parent: animation,
-                            curve: Curves.easeOutCubic,
+                            curve: Curves.easeOutQuart,
                           );
                           return FadeTransition(
                             opacity: Tween<double>(
-                              begin: .82,
+                              begin: .94,
                               end: 1,
                             ).animate(curved),
-                            child: SlideTransition(
-                              position: Tween<Offset>(begin: offset, end: Offset.zero)
-                                  .animate(curved),
-                              child: child,
+                            child: ScaleTransition(
+                              scale: Tween<double>(begin: .992, end: 1)
+                                  .animate(curved),
+                              child: SlideTransition(
+                                position: Tween<Offset>(
+                                  begin: offset,
+                                  end: Offset.zero,
+                                ).animate(curved),
+                                child: child,
+                              ),
                             ),
                           );
                         },
@@ -2264,6 +2271,8 @@ class _SettingsCard extends StatelessWidget {
     decoration: BoxDecoration(
       color: _surface,
       borderRadius: BorderRadius.circular(_radiusMd),
+      border: Border.all(color: const Color(0xFFF0F0F0)),
+      boxShadow: const <BoxShadow>[],
     ),
