--- a/lib/main.dart
+++ b/lib/main.dart
@@ -482,8 +482,8 @@ class _ArchiveShellState extends State<ArchiveShell> {
                       AnimatedSwitcher(
-                        duration: _motionDuration(context, 230),
-                        reverseDuration: _motionDuration(context, 200),
+                        duration: _motionDuration(context, 250),
+                        reverseDuration: _motionDuration(context, 215),
@@ -496,9 +496,9 @@ class _ArchiveShellState extends State<ArchiveShell> {
                           final reverse = key is ValueKey<_PageTransitionToken>
                               ? key.value.reverse
                               : false;
                           final offset = reverse
-                              ? const Offset(-.045, 0)
-                              : const Offset(.045, 0);
+                              ? const Offset(-.032, 0)
+                              : const Offset(.032, 0);
@@ -504,7 +504,7 @@ class _ArchiveShellState extends State<ArchiveShell> {
                           return FadeTransition(
                             opacity: Tween<double>(
-                              begin: .82,
+                              begin: .76,
                               end: 1,
