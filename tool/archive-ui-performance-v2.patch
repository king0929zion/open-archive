--- a/lib/main.dart
+++ b/lib/main.dart
@@
   WidgetsFlutterBinding.ensureInitialized();
   PaintingBinding.instance.imageCache.maximumSize = 120;
   PaintingBinding.instance.imageCache.maximumSizeBytes = 64 << 20;
+  await SystemChrome.setEnabledSystemUIMode(SystemUiMode.edgeToEdge);
   SystemChrome.setSystemUIOverlayStyle(
     const SystemUiOverlayStyle(
-      statusBarColor: _bg,
+      statusBarColor: Colors.transparent,
       statusBarIconBrightness: Brightness.dark,
-      systemNavigationBarColor: _bg,
+      statusBarBrightness: Brightness.light,
+      systemNavigationBarColor: Colors.transparent,
       systemNavigationBarIconBrightness: Brightness.dark,
+      systemNavigationBarDividerColor: Colors.transparent,
     ),
   );
@@
                           return FadeTransition(
                             opacity: Tween<double>(
-                              begin: .82,
+                              begin: .72,
                               end: 1,
                             ).animate(curved),
                             child: SlideTransition(
                               position: Tween<Offset>(
                                 begin: offset,
                                 end: Offset.zero,
                               ).animate(curved),
-                              child: child,
+                              child: ScaleTransition(
+                                scale: Tween<double>(
+                                  begin: .988,
+                                  end: 1,
+                                ).animate(curved),
+                                alignment: reverse
+                                    ? Alignment.centerLeft
+                                    : Alignment.centerRight,
+                                child: child,
+                              ),
                             ),
                           );
@@
     return GlassIconButton(
       icon: icon,
       onPressed: callback,
       size: size,
       iconSize: size * .47,
-      interactionScale: .94,
+      interactionScale: .925,
       useOwnLayer: true,
-      glowColor: dark ? const Color(0x30111111) : const Color(0x4DFFFFFF),
-      glowRadius: size * .58,
+      glowColor: dark ? const Color(0x24111111) : const Color(0x38FFFFFF),
+      glowRadius: size * .66,
       settings: LiquidGlassSettings(
-        blur: 10,
-        thickness: dark ? 30 : 24,
-        glassColor: dark ? const Color(0xD4222222) : const Color(0x52FFFFFF),
-        backerColor: dark ? const Color(0xD9222222) : const Color(0xC7F8F8F8),
-        glowIntensity: dark ? .62 : .48,
-        shadowElevation: dark ? .7 : .45,
-        saturation: 1.12,
+        blur: dark ? 7 : 5.5,
+        thickness: dark ? 25 : 19,
+        glassColor: dark ? const Color(0x7A181818) : const Color(0x24FFFFFF),
+        backerColor: dark ? const Color(0x52111111) : const Color(0x14FFFFFF),
+        lightIntensity: dark ? .58 : .72,
+        ambientStrength: dark ? .06 : .1,
+        ambientRim: dark ? .16 : .22,
+        fresnelStrength: dark ? .78 : .9,
+        refractiveIndex: 1.16,
+        chromaticAberration: .008,
+        glowIntensity: dark ? .42 : .3,
+        specularSharpness: GlassSpecularSharpness.sharp,
+        standardOpacityMultiplier: dark ? .72 : .42,
+        shadowElevation: dark ? .45 : .28,
+        saturation: 1.16,
+        whitenStrength: dark ? 0 : .025,
       ),
       anchorStretch: true,
       semanticLabel: iconName,
@@
   void _select(AppPage page) {
     widget.onClose();
-    Future<void>.delayed(const Duration(milliseconds: 160), () {
+    Future<void>.delayed(const Duration(milliseconds: 130), () {
       if (mounted) widget.onSelect(page);
     });
   }
@@
           AnimatedOpacity(
-            opacity: progress,
-            duration: _dragging ? Duration.zero : _motionDuration(context, 220),
+            opacity: progress * .78,
+            duration: _dragging ? Duration.zero : _motionDuration(context, 190),
+            curve: Curves.easeOutCubic,
             child: GestureDetector(
               onTap: widget.onClose,
               child: const ColoredBox(
@@
           AnimatedContainer(
-            duration: _dragging ? Duration.zero : _motionDuration(context, 260),
-            curve: Curves.easeOutCubic,
-            transform: Matrix4.translationValues(-width * (1 - progress), 0, 0),
+            duration: _dragging ? Duration.zero : _motionDuration(context, 285),
+            curve: Curves.easeOutQuint,
+            transformAlignment: Alignment.centerLeft,
+            transform: Matrix4(
+              .985 + (.015 * progress), 0, 0, 0,
+              0, .985 + (.015 * progress), 0, 0,
+              0, 0, 1, 0,
+              -width * (1 - progress), 0, 0, 1,
+            ),
             child: GestureDetector(
               onHorizontalDragUpdate: (details) => _dragUpdate(details, width),
               onHorizontalDragEnd: _dragEnd,
@@
                 height: double.infinity,
                 padding: const EdgeInsets.fromLTRB(0, 28, 0, 16),
                 decoration: const BoxDecoration(
-                  color: _bg,
+                  color: Color(0xFCFFFFFF),
                   borderRadius: BorderRadius.horizontal(
-                    right: Radius.circular(_radiusLg),
+                    right: Radius.circular(32),
                   ),
+                  boxShadow: [
+                    BoxShadow(
+                      color: Color(0x16000000),
+                      blurRadius: 30,
+                      offset: Offset(8, 0),
+                    ),
+                  ],
                 ),
                 child: Column(
